```markdown
# DnsToHosts 🌐

**DnsToHosts** — это легковесная утилита на Java для массового резолвинга (разрешения) доменных имен в IP-адреса через протокол **DNS-over-HTTPS (DoH)** с последующим созданием файла `hosts`.

Главная особенность программы — ручная реализация **бинарного протокола DNS (RFC 8484)**. Это позволяет использовать утилиту с серверами, которые не поддерживают JSON API.

## ✨ Возможности

*   🚀 **Без зависимостей:** Написана на чистой Java 8. Никаких сторонних библиотек — только один `.jar` или `.exe` файл.
*   🛠 **Поддержка Binary DoH:** Работает с любыми DoH серверами, включая те, что поддерживают только `application/dns-message`.
*   📶 **Dual Stack:** Поддержка и IPv4 (записи A), и IPv6 (записи AAAA).
*   ⚙️ **Гибкая настройка:** Возможность смены DNS-сервера и отключения протоколов через конфиг.
*   📄 **Авто-сборка:** Автоматически объединяет найденные IP с внешним списком `extra.txt` в итоговый файл `host.txt`.

## 🚀 Быстрый старт

### 1. Подготовка
Скачайте релиз или соберите программу самостоятельно.
Создайте в папке с программой файл `input.txt` и добавьте туда список доменов (каждый с новой строки):

```text
# ChatGPT
chatgpt.com

# Google
google.com
```

### 2. Запуск
Запустите файл `DnsToHosts.exe` или используйте командную строку:

```bash
java -jar DnsToHosts.jar
```

### 3. Результат
После завершения работы в папке появится файл **`host.txt`**, содержащий готовые IP-адреса для использования.

## 🔧 Настройка (config.txt)

Файл `config.txt` создается автоматически при первом запуске. Вы можете отредактировать его любым текстовым редактором.

**Пример настройки для dns.google:**
```ini
server=https://dns.google/dns-query
ipv4=true
ipv6=true
```

**Пример настройки для AdGuard:**
```ini
server=https://dns.adguard-dns.com/dns-query
ipv4=true
ipv6=false
```
*(Если удалить файл config.txt, программа создаст новый с настройками Google DNS по умолчанию).*

## 📂 Структура файлов

*   `DnsToHosts.jar` / `.exe` — Исполняемый файл программы.
*   `input.txt` — Ваш список доменов (входные данные).
*   `config.txt` — Настройки сервера и протоколов.
*   `extra.txt` — (Опционально) Дополнительный файл, содержимое которого будет добавлено в конец результата.
*   `output.txt` — Промежуточный файл с результатами сканирования.
*   **`host.txt`** — Итоговый результат.

## 🛠 Сборка из исходного кода

Для сборки вам понадобится JDK 8 или выше.

1.  Сохраните код в файл `DnsToHosts.java`.
2.  Скомпилируйте класс:
    ```bash
    javac -encoding UTF-8 DnsToHosts.java
    ```
3.  Соберите JAR-архив:
    ```bash
    jar cfe DnsToHosts.jar DnsToHosts *.class
    ```

## 👤 Авторы

*   **antonlosk** — Идея, тестирование, сборка и поддержка репозитория.
*   **OpenAI ChatGPT** — Написание исходного кода (Java) и документации.
```
